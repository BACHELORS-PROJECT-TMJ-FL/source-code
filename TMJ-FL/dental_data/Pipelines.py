import datasets

def entire_data_processing_pipeline(config, rootPath: str = "", verbose = False):

    print("The data processing pipeline is not available, like the dataset itself, it is not available in the public repository.")
    
    dataset = datasets.Dataset.from_dict({
        "features": [],
        "labels": [],
    })

    
    return dataset
    